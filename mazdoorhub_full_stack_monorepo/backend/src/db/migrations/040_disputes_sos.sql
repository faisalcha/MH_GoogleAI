CREATE TABLE IF NOT EXISTS disputes ( id uuid PRIMARY KEY DEFAULT gen_random_uuid(), job_id uuid NOT NULL, opened_by uuid NOT NULL, reason text, status varchar(16) NOT NULL DEFAULT 'OPEN', created_at timestamptz NOT NULL DEFAULT now()); CREATE TABLE IF NOT EXISTS sos_events ( id uuid PRIMARY KEY DEFAULT gen_random_uuid(), user_id uuid NOT NULL, lat numeric, lng numeric, status varchar(16) NOT NULL DEFAULT 'OPEN', created_at timestamptz NOT NULL DEFAULT now());